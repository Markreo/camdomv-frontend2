import {BaseEntity} from '../../base/_models/base.entity';

export class InterestPaymentEntity extends BaseEntity {
  constructor(entity: Partial<InterestPaymentEntity> = {}) {
    super(entity);
  }
}
